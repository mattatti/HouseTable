server:

set up a database e.g postgres
set up your .env file according to the .env example

Then in the terminal enter these commands:
npm install
npm run dev

client:

In the terminal enter these commands:
npm install
npm start

